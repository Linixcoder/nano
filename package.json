{
  "name": "nano",
  "description": "A minimalist web proxy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'No build needed' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
